import { describe, it } from 'vitest'

describe('Users tests', () => {
  it.todo('should be able to create a new USER')

  it.todo(
    'should be able NOT be able to register a USER with an email already register by another USER',
  )

  it.todo(
    'should be able to create get USER metrics ("meals_amount", "meals_onDiet_amount", "meals_offDiet_amount", "meals_onDiet_sequence")',
  )
})
