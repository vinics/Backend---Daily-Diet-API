import { describe, it } from 'vitest'

describe('Sessions tests', () => {
  it.todo('should be able to authenticate an USER')

  it.todo('should be able to identify a logged USER')

  it.todo(
    'should be able NOT be able to authenticate an USER using wrong credentials',
  )

  it.todo('should be able NOT be able to authenticate an non-registered USER')
})
